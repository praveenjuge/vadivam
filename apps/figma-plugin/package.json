{
  "name": "@vadivam/figma-plugin",
  "version": "0.0.28",
  "private": true,
  "type": "module",
  "description": "Search and insert canonical Vadivam icon components in Figma.",
  "scripts": {
    "build": "bun scripts/build.mjs",
    "dev": "bun scripts/build.mjs --watch",
    "test": "bun run build && bun test --preload ./test/setup.ts && bun run typecheck",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@figma/plugin-typings": "1.131.0",
    "@happy-dom/global-registrator": "^20.9.0",
    "@types/bun": "^1.3.14",
    "esbuild": "0.28.1",
    "typescript": "7.0.2"
  }
}
